# Rentaloop 站內信系統實施方案

## 📋 專案概述

### 系統目標和核心需求
為 Rentaloop 平台建立完整的站內信系統，包含三個核心子系統：
1. **站內信基礎系統** - 租貸相關的即時通訊功能
2. **圖片管理擴展** - 臨時圖片的過期機制和自動清理
3. **延遲郵件通知** - 20分鐘延遲機制的智能郵件通知

### 技術架構總覽
- **前端**：React + TypeScript + Tailwind CSS
- **後端**：Next.js Server Actions
- **資料庫**：PostgreSQL + Drizzle ORM
- **緩存/隊列**：Redis (過期機制和延遲任務)
- **郵件服務**：Resend
- **圖片服務**：Cloudinary

### 實施範圍說明
- 租貸相關的私人通訊功能
- 臨時圖片的 2 週自動過期機制
- 20 分鐘未讀延遲郵件通知
- 3MB 圖片大小限制和自動 WebP 轉換

---

## 🎯 第一部分：站內信基礎系統

### 1.1 核心功能需求
- **租貸相關溝通場景**：租客與業主就特定租賃進行私人通訊
- **即時通訊需求**：支援 WebSocket/SSE 的實時消息推送
- **消息存儲管理**：1 年後自動清理舊消息，用戶可手動刪除

### 1.2 實施計劃

**第一階段：基礎架構擴展 (1-2天)**
- 資料庫 Schema 優化
- API 端點建立

**第二階段：後端 Server Actions (2-3天)**
- 消息處理 Actions
- 通知系統 Actions

**第三階段：前端組件開發 (3-4天)**
- 聊天介面組件
- 通知中心組件
- 整合到現有頁面

**第四階段：即時通訊實現 (2-3天)**
- WebSocket 整合
- 實時更新機制

**第五階段：測試和優化 (1-2天)**
- 功能測試
- 性能優化

### 1.3 關鍵技術組件

**消息類型定義**
```typescript
enum MessageType {
  TEXT = 'text',
  IMAGE = 'image',
  SYSTEM = 'system'
}

enum NotificationType {
  RENTAL_REQUEST = 'rental_request',
  RENTAL_APPROVED = 'rental_approved',
  RENTAL_COMPLETED = 'rental_completed',
  SYSTEM_ANNOUNCEMENT = 'system_announcement'
}
```

---

## 🖼️ 第二部分：圖片管理擴展

### 2.1 圖片處理需求
- **3MB 大小限制**：從現有的 5MB 調整為 3MB
- **2 週自動過期機制**：使用 Redis TTL 自動清理
- **自動 WebP 轉換**：強制轉換為 WebP 格式以節省存儲空間

### 2.2 Redis 過期機制
- **臨時圖片分類管理**：永久圖片 vs 臨時圖片
- **Redis Key 設計**：`temp_img:${imageId}` 存儲元數據
- **自動清理流程**：Redis 過期事件觸發 Cloudinary 刪除

### 2.3 Cloudinary 優化
```typescript
// 臨時圖片專用配置
cloudinary.uploader.upload_stream({
  folder: 'rentaloop/temp',
  format: 'webp',
  fetch_format: 'webp',
  quality: 'auto:good',
  transformation: [
    {
      width: 1200,
      crop: 'limit',
      fetch_format: 'webp',
      quality: 'auto:good',
      flags: 'progressive'
    }
  ],
  tags: ['temp_image', `user_${userId}`, `expires_${expireDate}`],
  invalidate: true,
  unique_filename: true,
  overwrite: false
});
```

### 2.4 實施計劃
- **第一階段**：基礎架構擴展 (2天)
- **第二階段**：臨時圖片上傳 (2-3天)
- **第三階段**：自動清理機制 (2-3天)
- **第四階段**：監控和優化 (1-2天)

---

## 📧 第三部分：延遲郵件通知系統

### 3.1 系統設計
- **20 分鐘延遲機制**：訊息發送後 20 分鐘未讀才發送郵件
- **防亂發保護措施**：用戶在 20 分鐘內讀取則取消郵件發送
- **訊息發送即取消邏輯**：已讀訊息的郵件通知自動取消

### 3.2 技術架構
**系統流程圖**
```
用戶A發送站內信
    ↓
【步驟1】儲存訊息到資料庫
    ↓
【步驟2】設定延遲通知任務 (Redis TTL: 1200秒)
    ↓
【並發流程A - 用戶B讀取訊息】
    └→ 刪除 Redis Key (取消郵件發送)
    └→ 更新訊息為已讀狀態
    ↓
【並發流程B - 20分鐘後觸發】
    └→ Redis 自動觸發過期事件
    └→ 檢查訊息是否已讀
    └→ 如果未讀 → 發送郵件通知
```

### 3.3 實施計劃
- **第一階段**：資料庫 Schema 擴展 (1天)
- **第二階段**：延遲通知機制 (2-3天)
- **第三階段**：站內信功能 (2-3天)
- **第四階段**：定時清理任務 (1-2天)
- **第五階段**：前端組件整合 (2-3天)

### 3.4 關鍵代碼組件

**延遲郵件管理器**
```typescript
export class DelayedEmailManager {
  private static readonly EMAIL_DELAY_SECONDS = 20 * 60; // 20 分鐘
  
  static async scheduleEmailNotification(data: PendingEmailData): Promise<void> {
    const redis = getRedis();
    if (!redis) return;

    const key = `pending_email:${data.messageId}`;
    const value = JSON.stringify(data);
    
    // 設置 20 分鐘後過期
    await redis.setex(key, this.EMAIL_DELAY_SECONDS, value);
    
    // 記錄到資料庫
    await db.insert(emailNotificationLogs).values({
      messageId: data.messageId,
      recipientUserId: data.recipientUserId,
      recipientEmail: data.recipientEmail,
      rentalId: data.rentalId,
      senderUserId: data.senderUserId,
      status: 'pending',
      scheduledAt: new Date(data.scheduledAt),
    });
  }
  
  static async cancelEmailNotification(messageId: string): Promise<void> {
    const redis = getRedis();
    if (!redis) return;

    const key = `pending_email:${messageId}`;
    await redis.del(key);
    
    // 更新資料庫記錄
    await db.update(emailNotificationLogs)
      .set({ 
        status: 'cancelled',
        cancelledAt: new Date()
      })
      .where(eq(emailNotificationLogs.messageId, messageId));
  }
}
```

**站內信發送 Action**
```typescript
export async function sendMessage(rentalId: string, content: string) {
  const session = await auth();
  if (!session?.user?.id) {
    return { error: '未授權' };
  }

  try {
    // 儲存訊息
    const message = await db.insert(rentalMessages).values({
      rentalId,
      senderId: session.user.id,
      content,
      createdAt: new Date(),
    }).returning();

    // 獲取接收者資訊並排程延遲郵件通知
    const recipient = await db.query.users.findFirst({
      where: eq(users.id, recipientId)
    });

    if (recipient) {
      await DelayedEmailManager.scheduleEmailNotification({
        messageId: message[0].id,
        recipientUserId: recipientId,
        recipientEmail: recipient.email,
        rentalId,
        senderUserId: session.user.id,
        senderName: session.user.name || '會員',
        itemName: rental.item.title,
        messageCreatedAt: new Date().toISOString(),
        scheduledAt: new Date().toISOString(),
      });
    }

    return { success: true, message: message[0] };
  } catch (error) {
    console.error('Failed to send message:', error);
    return { error: '發送訊息失敗' };
  }
}
```

---

## 🏗️ 第四部分：整合技術架構

### 4.1 資料庫 Schema
```sql
-- 擴展 rental_messages 表
ALTER TABLE rental_messages 
ADD COLUMN IF NOT EXISTS email_notification_scheduled BOOLEAN DEFAULT FALSE,
ADD COLUMN IF NOT EXISTS email_sent BOOLEAN DEFAULT FALSE,
ADD COLUMN IF NOT EXISTS email_sent_at TIMESTAMP,
ADD COLUMN IF NOT EXISTS is_read BOOLEAN DEFAULT FALSE,
ADD COLUMN IF NOT EXISTS read_at TIMESTAMP;

-- 新增郵件發送日誌表
CREATE TABLE email_notification_logs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    message_id UUID NOT NULL REFERENCES rental_messages(id) ON DELETE CASCADE,
    recipient_user_id TEXT NOT NULL REFERENCES users(id),
    recipient_email TEXT NOT NULL,
    rental_id UUID REFERENCES rentals(id),
    sender_user_id TEXT REFERENCES users(id),
    status VARCHAR(20) NOT NULL,
    scheduled_at TIMESTAMP NOT NULL,
    sent_at TIMESTAMP,
    cancelled_at TIMESTAMP,
    error_message TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);

-- 新增臨時圖片管理表
CREATE TABLE temp_images (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id TEXT NOT NULL,
    cloudinary_public_id TEXT NOT NULL UNIQUE,
    original_filename TEXT,
    file_size BIGINT NOT NULL,
    upload_date TIMESTAMP DEFAULT NOW(),
    expires_at TIMESTAMP NOT NULL,
    is_deleted BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT NOW()
);
```

### 4.2 Redis 結構設計
```typescript
// Redis Key 設計
interface RedisKeys {
  pendingEmail: `pending_email:${messageId}`;           // 待發送的郵件通知
  tempImage: `temp_img:${imageId}`;                      // 臨時圖片元數據
  emailQueue: 'email_notification_queue';                 // 郵件通知隊列
}

// 延遲郵件數據結構
interface PendingEmailData {
  messageId: string;
  recipientUserId: string;
  recipientEmail: string;
  rentalId: string;
  senderUserId: string;
  senderName: string;
  itemName: string;
  messageCreatedAt: string;
  scheduledAt: string;
}
```

### 4.3 API 端點規劃
```typescript
// 消息相關 API
app/api/messages/[id]/read/route.ts           // 標記已讀
app/api/rentals/[id]/messages/route.ts        // 租賃訊息列表

// 圖片上傳 API
app/api/upload/temp/route.ts                  // 臨時圖片上傳

// Cron Job 端點
app/api/cron/process-delayed-emails/route.ts  // 處理延遲郵件
app/api/cron/cleanup-temp-images/route.ts     // 清理臨時圖片
```

---

## 📅 實施時間表

### 總體時間規劃
- **站內信系統**：9-14天
- **圖片管理擴展**：7-10天
- **延遲郵件通知**：8-12天
- **整合測試和上線**：3-5天

### 里程碑設定
- **第 10 天**：站內信基礎功能上線
- **第 15 天**：圖片過期機制完成
- **第 20 天**：延遲郵件通知系統上線
- **第 25 天**：全系統整合測試完成

---

## 🔒 安全和最佳實踐

### 5.1 安全性考量
- **訪問控制機制**：只有租賃相關方才能發送/查看訊息
- **輸入驗證策略**：訊息內容長度限制、格式驗證
- **權限管理**：基於用戶角色的功能權限控制

### 5.2 性能優化
- **批量處理優化**：一次處理多個圖片或郵件，避免 API 限制
- **緩存策略**：Redis 緩存用戶會話和臨時數據
- **並發控制**：避免同時執行多個清理任務

### 5.3 監控維護
- **日誌記錄**：完整的操作日誌和錯誤追蹤
- **異常處理**：自動重試機制和降級策略
- **健康檢查**：Redis 連接狀態和系統性能監控

---

## ⚠️ 注意事項

### 6.1 關鍵依賴
- **Redis 可用性**：確保 Redis 服務穩定運行
- **Resend 配置**：正確配置 API 密鑰和發送域名
- **Cloudinary 設置**：驗證臨時圖片資料夾和轉換配置

### 6.2 實施風險
- **技術風險**：Redis 過期機制的可靠性
- **時間風險**：並行開發可能導致的延期
- **整合風險**：與現有系統的兼容性問題

### 6.3 驗證清單
- **功能測試**：消息發送接收、郵件延遲、圖片過期
- **性能測試**：並發用戶和大量數據的處理能力
- **安全測試**：權限控制和數據保護機制驗證

---

## 📈 預期效果

### 用戶價值
- **提升通訊效率**：即時消息通知和便捷回覆機制
- **降低溝通成本**：整合的通訊平台減少工具切換
- **增強平台粘性**：完整的租賃生態系統體驗

### 商業價值
- **減少客服工作量**：自動化的通知和溝通機制
- **提高租賃成功率**：及時的通訊促進交易達成
- **收集用戶數據**：通訊模式分析優化平台功能

### 系統價值
- **存儲優化**：自動清理機制節省存儲成本
- **性能提升**：減少無效數據對系統性能的影響
- **可維護性**：標準化的通知和消息管理機制

---

*本文檔將隨著實施進度持續更新，確保技術方案的準確性和可執行性。*